const React = require('react');

const CreateGist = React.createClass({
  render : function(){
    return (
      <div className="Add Gist">
        <h2>Create Gist</h2>
      </div>
    )
  }
});

module.exports = CreateGist;